# scripts/

Standalone utility scripts that don't belong to a specific stack or host — one-off or recurring helpers (e.g. bulk operations, migration helpers, data fixes).

**Status:** empty for now. Will collect scripts as they're written rather than being pre-populated.
