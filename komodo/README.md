# komodo/

Komodo's own Resource Sync configuration (TOML), version-controlled alongside everything it manages.

**Status:** empty. Komodo doesn't exist yet — deployment is planned for Sprint 2. See `docs/adrs/0002-komodo-for-docker-gitops.md` for the reasoning and rollout plan.
