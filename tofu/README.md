# tofu/

OpenTofu modules for VM/CT provisioning (L1 — Provisioning), using the `bpg/proxmox` provider.

**Status:** empty. Deferred conditionally to Sprint 7 — provisioning automation has a weaker ROI at current host-creation frequency than configuration (L2) or deployment (L3), so it's deliberately last in the sequence rather than skipped outright.
